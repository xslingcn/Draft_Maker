import assert from "assert";
import router from "../../../../routes/draft.route";

describe('routes/draftRoute', function () {
    it('router exists', function () {
        assert.notStrictEqual(router, undefined);
    });
});